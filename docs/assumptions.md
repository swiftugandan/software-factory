# Assumptions Ledger

Every non-obvious decision the factory made instead of stopping to ask. Review the
`one-way` rows first — each names the flag or interface that lets you change it.

| # | Gap in source docs | Assumption made | Reversibility | BDD basis | Owner | Status |
|---|---|---|---|---|---|---|
| 1 | BDD does not specify the short code format, length, or alphabet | Codes are 7-character base62 [0-9A-Za-z], generated randomly and checked for uniqueness on insert; alphabet/length live behind config so they can change for future codes without migrating existing ones | cheap | BDD Core behaviors | refine-product-manager | ok |
| 2 | BDD does not state whether short codes must be globally unique or how collisions are handled | The short code column carries a unique constraint; generation retries on collision; uniqueness is a permanent data-model invariant | one-way | BDD Core behaviors | refine-product-manager | APPROVED |
| 3 | BDD does not define what counts as a valid long URL | Accept only absolute http:// or https:// URLs up to 2048 chars; reject anything else with a validation error. Scheme allowlist behind config | cheap | BDD Core behaviors (long URL) | refine-product-manager | ok |
| 4 | BDD does not define what a 'click' is or how counts are stored | Each successful redirect from GET /{code} records one click event row; total clicks is the count of those rows for the link. Click-event granularity is a data-model choice captured in a click_events table (vs a bare counter) | one-way | BDD Core behaviors (total clicks) | refine-product-manager | APPROVED |
| 5 | BDD does not specify the HTTP redirect status for /{code} | Use 302 Found (temporary) so intermediaries do not cache and skip click counting | cheap | BDD Core behaviors (redirect) | refine-product-manager | ok |
| 6 | BDD does not say whether visiting /{code} requires sign-in | The redirect endpoint /{code} is public; only link creation and the link list require authentication | cheap | BDD Users / Core behaviors | refine-product-manager | ok |
| 7 | BDD does not say what happens for an unknown or malformed code | Unknown or malformed code returns HTTP 404 and records no click | cheap | BDD Core behaviors (redirect) | refine-product-manager | ok |
| 8 | BDD does not specify behavior when a member submits the same long URL twice | Each submission creates a new, distinct short link with its own code (no dedupe); simpler and avoids surprising shared-count semantics | cheap | BDD Core behaviors (create) | refine-product-manager | ok |
| 9 | BDD does not specify the authentication method | Email/password plus OAuth per factory convention; members act only on their own links | cheap | CLAUDE.md conventions; BDD Users | refine-product-manager | ok |
| 10 | BDD does not specify ordering or pagination of the member link list | List shows the member's own non-deleted links, newest first, paginated at 25 per page | cheap | BDD Core behaviors (list) | refine-product-manager | ok |
| 11 | BDD does not mention link deletion but CLAUDE.md mandates soft deletes for user-owned rows | Links carry a deleted_at column; deletion is out of scope for this release but the schema supports soft delete, and soft-deleted links are excluded from lists and resolve to 404 | one-way | CLAUDE.md conventions | refine-product-manager | APPROVED |
| 12 | PRD AC-6 requires retrying code generation until unique but does not bound retry attempts or define behavior if generation is exhausted under pathological contention | Cap collision retries at a configurable max (default 10) per creation attempt; hitting the cap returns HTTP 503 with a generic retry-later error and persists no row (AC-6's uniqueness invariant is never violated). Cap lives in config/link-generation so it can be raised without a code change. | cheap | PRD AC-6 | refine-workflow-architect | ok |
| 13 | PRD AC-19 requires pagination at 25/page but does not define behavior for an invalid, out-of-range, or malformed page parameter on the list endpoint | A page number past the last page returns HTTP 200 with an empty collection, consistent with AC-20's empty-is-not-an-error precedent; a malformed or non-numeric page parameter is normalized to page 1 rather than erroring, least surprise for a read-only list. | cheap | PRD AC-19, AC-20 | refine-workflow-architect | ok |
| 14 | PRD AC-2 names email/password and OAuth as the auth methods but neither PRD nor BDD defines the sign-in failure paths: wrong password, unknown email, OAuth provider error, or user cancellation | Invalid email/password re-renders sign-in with a generic 'invalid email or password' message (no account enumeration) and creates no session; an OAuth error or cancel returns the member to sign-in with a generic 'sign-in was not completed' notice. No partial session or account is ever created on a failed attempt. | cheap | PRD AC-1, AC-2 | refine-workflow-architect | ok |
| 15 | PRD AC-8 requires the create response to include a fully-qualified short URL, but neither PRD nor BDD names the host/domain used to build it | The short URL is built by prefixing the stored code with a configured base host (config/app SHORT_LINK_BASE_URL); the host is environment-configurable and never hardcoded, so it can change per deployment without a data migration since stored codes are host-independent. | cheap | PRD AC-8 | refine-workflow-architect | ok |
| 16 | PRD AC-1 specifies unauthenticated UI requests to member pages redirect to sign-in, but not whether the member returns to the originally requested page after authenticating | The sign-in redirect preserves the originally requested member page via a validated in-app-only return-to parameter, so a successful sign-in lands the member back where they asked to go; an absent or invalid return-to defaults to the link list. | cheap | PRD AC-1 | refine-workflow-architect | ok |
| 17 | PRD AC-9/AC-11 define redirect resolution by the {code} path segment only; neither doc says whether a query string or fragment on the short URL affects code lookup or is forwarded to the destination | A query string or fragment on GET /{code} is ignored for code lookup (only the path segment is matched) and is not appended to the stored long URL; the Location header returns the destination exactly as stored. | cheap | PRD AC-9, AC-11 | refine-workflow-architect | ok |
| 18 | PRD AC-2 and Ledger #9 require OAuth as a sign-in method, but neither the BDD, PRD, nor workflows name which OAuth provider(s) TinyLink must support, and build tasks need a concrete target to implement WF-1b | Implement a single default OAuth provider (Google) for this release, behind a pluggable provider interface (config/auth providers) so additional providers can be added later without a schema or workflow change; oauth_identities table is provider-agnostic (provider + provider_subject columns) | cheap | PRD AC-2; Ledger #9; WF-1b | refine-task-planner | ok |
| 19 | WF-1 and the PRD define sign-in (existing-account authentication) but no acceptance criterion or workflow covers self-service account registration/provisioning for email/password members; task sequencing needs to know how a member row first comes to exist | Self-service registration is out of scope for this build (no PRD criterion requires it). OAuth auto-provisions a member record on first successful login (standard OAuth pattern). Email/password accounts are provisioned out-of-band (seed/admin script) for this release; the members.password_hash column stays nullable so a registration endpoint can be added later without a migration | cheap | PRD Acceptance criteria (no AC for registration); BDD Notes ("signed-in users") | refine-task-planner | ok |
| 20 | PRD/BDD name no implementation language, runtime, or web framework for the backend | Node.js 22 LTS with plain JavaScript (ESM) and Express 5; chosen for a lean, no-build stack the factory tooling (npm/node --test, mutation-probe) targets natively. Language is a code-level choice with no data-model impact; see ADR-0001/0002 | cheap | PRD (no stack named); CLAUDE.md | build-software-architect | ok |
| 21 | PRD does not say whether the UI is a SPA or server-rendered | Server-rendered EJS templates served by the same Express process; no SPA/bundler, keeping the small app lean. View layer is swappable behind the same JSON/HTTP API; see ADR-0003 | cheap | PRD G1-G4; scope is 4 pages | build-software-architect | ok |
| 22 | PRD names Postgres but not the DB access library or migration tool | node-postgres (pg) driver with hand-written parameterized SQL (no ORM) and node-pg-migrate for schema migrations against Postgres 16. Access layer is isolated in a repository module; see ADR-0004 | cheap | PRD AC-24; CLAUDE.md (Postgres) | build-software-architect | ok |
| 23 | No table id-type / schema convention specified | bigint GENERATED ALWAYS AS IDENTITY primary keys, snake_case names, timestamptz stored in UTC, FKs ON DELETE RESTRICT; codes stay char-based and host-independent; see ADR-0005 | cheap | CLAUDE.md (UTC, Postgres); PRD AC-22 | build-software-architect | ok |
| 24 | No test framework, coverage tool, or CI test command specified | Node built-in node:test + node:assert for unit and integration tests, supertest for HTTP, c8 for coverage (lcov+text, threshold 0.7); the factory mutation-probe runs the same testCommand. CI command: node --test --test-concurrency=1 tests/; see ADR-0007 | cheap | config/factory.json thresholds; CLAUDE.md | build-software-architect | ok |
| 25 | Ledger #9/#18 fix email/password + OAuth but not the session mechanism or password-hash algorithm | Server-side sessions (sessions table) keyed by an httpOnly, Secure, SameSite=Lax cookie; passwords hashed with Node crypto scrypt (no native build); OAuth via a pluggable provider interface (Google default). Hash algorithm and provider live behind config seams; see ADR-0009 | cheap | PRD AC-1/AC-2; Ledger #9,#18,#19 | build-software-architect | ok |
| 26 | Task instructions state Postgres peer auth works for OS user root, but no Postgres role named 'root' existed (only the 'postgres' superuser role), so postgresql:///tinylink connections and createdb failed with 'role "root" does not exist' | Created a Postgres role 'root LOGIN CREATEDB' (via the postgres superuser) so peer auth for the OS/CI user 'root' works as ADR-0004/T003 assume, then created the tinylink and tinylink_test databases owned by that role. This is a local/CI environment setup step, not an application change; reversible by dropping the role | cheap | ADR-0004 (peer-auth socket DSN); T003 CI instructions | build-devops | ok |
| 27 | ADR-0007's exact test command 'node --test --test-concurrency=1 tests/' (a bare directory path) does not recurse into tests/ in this environment's Node v22.22.2 — it errors with MODULE_NOT_FOUND trying to require the directory itself; only a glob pattern (tests/**/*.test.js) or bare 'node --test' (default discovery) works | package.json's test/coverage scripts use the glob form node --test --test-concurrency=1 "tests/**/*.test.js" instead of the literal 'tests/' from ADR-0007, same taxonomy/concurrency/coverage thresholds, functionally equivalent. config/factory.json's testCommand (used by mutation-probe.sh in /harden) still reads the literal 'tests/' form and should be updated the same way by whoever next touches it (build-software-architect/ADR-0007 owner) to avoid a broken /harden run | cheap | ADR-0007 (exact commands section); observed Node 22.22.2 behavior in this sandbox | build-devops | ok |
| 28 | T004/ADR-0005 require members.email UNIQUE but leave case sensitivity unspecified ('email citext/varchar unique' per task) | Use Postgres citext for members.email so uniqueness/lookup is case-insensitive (Foo@x.com and foo@x.com collide), matching Ledger #14's no-enumeration intent and common email-uniqueness practice. Requires CREATE EXTENSION citext, added idempotently in the T004 migration | cheap | ADR-0005 (members.email UNIQUE); T004 task spec | build-database | ok |
| 29 | ADR-0009 wants the session cookie to carry an opaque, random (>=128 bit) session id, but ADR-0005's convention makes every table's id a sequential bigint identity column; T006's scoped column list (id, member_id, created_at, expires_at) does not include a dedicated random token column | T006 migration implements exactly the scoped columns (sequential bigint id, member_id, created_at, expires_at) and leaves generation of the actual opaque/random bearer token to build-backend's T009 session helper. Flagging so T009 does not use the sequential id as the cookie value; if a random token is needed it can be added as a new UNIQUE column via a follow-up migration with no data loss | cheap | ADR-0009 (session transport); ADR-0005 (id convention); T006 task spec | build-database | ok |
| 30 | package.json sets "type": "module", but node-pg-migrate's generated migration template and MigrationBuilder API use CommonJS exports.up/exports.down; node-pg-migrate loads migration files via createRequire(), which throws 'exports is not defined in ES module scope' for a plain .js file under type:module | Migration files in migrations/ use the .cjs extension (not .js) so Node's loader treats them as CommonJS regardless of the package's ESM type field. Future migrations created with 'npm run migrate create' should be renamed .js -> .cjs, or the CLI invoked with '-j js' output manually renamed, until/unless node-pg-migrate's ESM support is adopted | cheap | ADR-0004 (node-pg-migrate migrations); ADR-0001 (ESM, no build step) | build-database | ok |
| 31 | Task instructions specify DATABASE_URL=postgres:///tinylink_test for unix-socket peer auth, but the 'pg' driver (unlike psql) resolves an omitted host to the pg library default 'localhost' (TCP, requiring scram-sha-256 password auth per pg_hba.conf) and an omitted user to process.env.USER, which is unset in this sandbox shell — so the literal DATABASE_URL alone fails to connect via node-pg-migrate/pg | Invoke node-pg-migrate (and, by extension, the app's pool) with USER=root and PGHOST=/var/run/postgresql set alongside DATABASE_URL=postgres:///tinylink_test so 'pg' resolves the same unix-socket peer-auth path psql uses by default. This is an environment/CI variable, not a schema or code change; build-devops (T002/T003) should ensure these are exported wherever DATABASE_URL is used in this sandbox/CI | cheap | ADR-0004 (peer-auth socket DSN); task instructions (DATABASE_URL=postgres:///tinylink_test) | build-database | ok |
| 32 | ADR-0009 fixes the session cookie flags as HttpOnly; Secure; SameSite=Lax; Path=/, but a browser (and supertest's cookie jar) never resends a Secure cookie over plain HTTP, which is how the app runs locally/in CI/tests (ADR-0007) | src/backend/services/session-service.js sets secure: true only when config.nodeEnv === 'production'; HttpOnly, SameSite=Lax, and Path=/ stay fixed as ADR-0009 requires. The flag reads from the existing config/app.js NODE_ENV seam, no new config surface | cheap | ADR-0009 (session transport); ADR-0007 (tests run over plain HTTP) | build-backend | ok |
| 33 | ADR-0009 mandates a sessions.expires_at UTC column but neither the PRD nor ADR-0009 states a session lifetime | src/backend/services/session-service.js issues sessions with a fixed 30-day expiry from issuance, a conservative default for a low-sensitivity link-management tool | cheap | ADR-0009 (sessions table expires_at); T009 task scope | build-backend | ok |
| 34 | ADR-0002 requires cookie parsing to go through Express but names no cookie-parsing package, and ADR-0001 keeps the dependency footprint minimal/no-build | Added no cookie-parser dependency; src/backend/lib/cookies.js reads the raw Cookie request header directly (a few lines). res.cookie() for *setting* cookies already works via Express's existing transitive 'cookie' dependency, so no new package.json dependency was needed | cheap | ADR-0001 (no build step, lean deps); ADR-0002 (cookie handling via Express) | build-backend | ok |
| 35 | T010/ADR-0008 do not define the POST /auth/sign-in success response body shape (only the error body is pinned by ADR-0008) | On success, POST /auth/sign-in returns HTTP 200 with { member: { id, email } }; the frontend sign-in page (T024) is expected to redirect client-side after reading this response rather than the backend issuing a 302 | cheap | ADR-0008 (error body pinned, success body not); T010 task scope | build-backend | ok |
| 36 | T011/ADR-0009 do not name a post-login redirect target for the OAuth callback (only WF-1b's UI-level Ledger #16 return-to applies to the frontend, T024, which does not exist yet) | GET /auth/oauth/:provider/callback redirects to /links on success (Ledger #16's default landing page when no return-to is present); validated in-app return-to preservation remains T024/frontend scope | cheap | Ledger #16 (default to link list); ADR-0009 (OAuth flow) | build-backend | ok |
| 37 | ADR-0009 says the OAuth provider client is injected for testability but does not require CSRF state-parameter replay verification, and no PRD/ADR criterion covers it for this single-provider, cookie-session app | oauth-service.js passes a fixed opaque state placeholder to the authorize URL and does not persist/verify it on callback; the seam (client.getAuthorizeUrl(state)) exists so per-request nonce verification can be added later with no interface change | cheap | ADR-0009 (OAuth provider seam); PRD AC-2 (no CSRF-state criterion) | build-backend | ok |
| 38 | ADR-0010/Ledger #12 caps short-code generation at CODE_RETRY_MAX 'retries' without specifying whether that is the total insert-attempt budget or the number of retries after the first attempt | Treat CODE_RETRY_MAX as the total insert-attempt budget (first attempt included); exhausting it throws RetryExhaustedError -> 503 with no row persisted | cheap | ADR-0010 (Ledger #12: 'caps retries (default 10) and returns 503 on exhaustion') | build-backend | ok |
| 39 | ADR-0002/PRD AC-12 require the redirect path's server time to be measurable against config.redirectP95BudgetMs, but neither names the mechanism for exposing that measurement to tests/ops | Redirect handler (src/backend/http/routers/redirect-router.js) times itself with src/backend/lib/server-timing.js and exposes elapsed ms via an X-Redirect-Server-Time-Ms response header on both the 302 and 404 paths, so tests assert it against config.redirectP95BudgetMs without external load tooling; swapping to real APM/tracing later only touches this router | cheap | ADR-0002 (redirect p95 budget); PRD AC-12 | build-backend | ok |
| 40 | Task instructions said EJS views/static live at src/backend/frontend/{views,public}, but ADR-0006 and the already-scaffolded repo place them at src/frontend/{views,public}; ADR-0003/ADR-0008 also leave open how the HTML dashboard/create/sign-in pages coexist with the JSON GET /links and POST /links API paths without colliding or being shadowed by the public GET /:code redirect | Followed the existing ADR-0006 layout: EJS templates in src/frontend/views, static assets in src/frontend/public, wired via app.set('views', ...)/express.static in src/backend/http/app.js. HTML pages are mounted at distinct /app/* paths (GET/POST /app/sign-in, GET/POST /app/links/new, GET /app/links) rather than sharing /links or /auth/* with the JSON API or using content-negotiation; this is the least-surprising option ADR-0003 names and keeps route registration order (redirect router still mounted last) trivially safe since /app/... is always a multi-segment path the single-segment GET /:code route can never match | cheap | ADR-0003 (frontend approach: distinct paths or content-negotiation); ADR-0006 (repository layout); ADR-0008 (route registration order) | build-frontend | ok |
| 41 | T011/Ledger #36 hardcoded the OAuth callback's success redirect to '/links' (the JSON GET /links API path), which predates the frontend existing; once T024-T028 add an HTML dashboard, redirecting a browser there after Google sign-in would render raw JSON instead of a page | Changed GET /auth/oauth/:provider/callback's success redirect target from '/links' to '/app/links' (the new HTML dashboard) in src/backend/http/routers/auth-router.js, and updated the one integration test asserting that literal redirect location. A validated in-app return-to param is NOT threaded through the OAuth provider round-trip (the authorize-URL/callback contract has no seam for it without deeper ADR-0009 changes); OAuth sign-in always lands on the link list, matching Ledger #16's stated default when no return-to applies. Email/password sign-in (T024) does honor return-to | cheap | Ledger #16 (default landing = link list); Ledger #36 (prior OAuth callback redirect target); ADR-0003 (HTML pages are a distinct view layer from the JSON API) | build-frontend | ok |
| 42 | Neither ADR-0003 nor the PRD specifies whether page routes call the JSON API endpoints internally or call the same service layer directly, nor whether static assets share the /:code redirect's root namespace | Page routes (src/backend/http/routers/pages-router.js) call authService/linksService directly (ADR-0003's 'thin page routes' option), never making an internal HTTP call to /auth/sign-in or /links, so there is one source of truth for validation/error mapping and no risk of the page/API contracts drifting. Static assets are served under /assets (express.static mounted at that prefix, not at '/'), so a short code can never collide with a static filename and vice versa | cheap | ADR-0003 (thin page routes calling the same service layer); ADR-0002/ADR-0008 (redirect route registration/namespace safety) | build-frontend | ok |
