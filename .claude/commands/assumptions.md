---
description: Show the assumptions ledger, highlighting one-way decisions needing review.
allowed-tools: Read, Bash
---

Read `docs/assumptions.md` and present it. Lead with the `REVIEW` (one-way) rows — for each,
state the decision and the flag or interface that lets a human change it. Then summarize the
`cheap` rows briefly. If the ledger is empty, say so; that either means refinement hasn't run
or the docs left no gaps.
